"""SQLAlchemy ORM models — database table definitions.

These are infrastructure-level classes mapping to PostgreSQL tables.
They are NOT domain entities; domain entities live in `app/core/entities/`.
"""
